# Issue Notes

Known follow-up areas:

- Keep package version aligned with changelog milestones.
- Prefer small direct commits when connector safety checks block larger edits.
- Run local checks after each batch.
- Add concrete implementations only after interface review.
- Keep UI pages focused on local review and planning until implementation boundaries are ready.
