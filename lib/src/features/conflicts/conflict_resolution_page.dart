import 'package:flutter/material.dart';import '../../models/sync_conflict.dart';import '../../services/sync_conflict_detector.dart';class ConflictResolutionPage extends StatelessWidget{ConflictResolutionPage({super.key});final conflicts=SyncConflictDetector().compare(localHashes:const {'devices.json':'local-a','settings.json':'same'},remoteHashes:const {'devices.json':'remote-a','settings.json':'same','manifest.json':'remote-b'});@override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(title:const Text('Conflicts')),body:ListView(padding:const EdgeInsets.all(16),children:[const Card(child:ListTile(leading:Icon(Icons.info_outline),title:Text('Conflict resolution scaffold'),subtitle:Text('Phase 9 shows decisions only. No sync write is executed.'))),...conflicts.map((c)=>Card(child:ListTile(leading:Icon(c.needsUserDecision?Icons.report_problem_outlined:Icons.compare_arrows_outlined),title:Text(c.path),subtitle:Text('${c.type.name} · local=${c.localHash??'-'} · remote=${c.remoteHash??'-'}'),trailing:c.type==SyncConflictType.bothChanged?const SegmentedButton<String>(segments:[ButtonSegment(value:'local',label:Text('Local')),ButtonSegment(value:'remote',label:Text('Remote'))],selected:{'local'}):null)))]));}
